<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteApp" parent="Theme.Material3.DayNight.NoActionBar">
    </style>
    <style name="Theme.NoteApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textColor">@color/black</item>
        <item name="android:windowBackground">@drawable/bg_application</item>
        <item name="fontFamily">@font/roboto_slab_medium</item>
        <item name="android:textSize">@dimen/default_text_size</item>
    </style>
    <style name="Theme.NoteApp.list" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

</resources>
